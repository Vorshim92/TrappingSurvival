---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by lele.
--- DateTime: 13/08/23 23:14
---

-- https://pzwiki.net/wiki/Trapping
-- SteamLibrary/steamapps/common/ProjectZomboid/projectzomboid/media/lua/server/Traps/TrapDefinition.lua

---@class Rat


local creatureFactory = require("CreatureFactory")
local rat = {}

local trap = require("Trap")
local bait = require("Bait")
local zone = require("Zone")

--- ---------------------- Start Rat Default values -------------------------------

--                  ** BAIT **
local cheese = 60
local chocolate = 30
local peanutButter = 40
local processedCheese = 55
local tomato = 35

--                  ** ZONE **
local deepForest = 10
local farmLand = 50
local forest = 10
local townZone = 30
local trailerPark = 40
local vegetation = 20

--                  ** TRAP **
local trapMouse = 25

--                  ** ANIMALS **
-- Type of animal
local type = "rat"

-- after how many hour the animal will start to destroy the cage/escape
local strength = 24

-- item given to the player
local item = "Base.DeadRat"

-- hour this animal will be out and when you can catch it
local minHour = 0
local maxHour = 0

-- min and max "size" (understand hunger reduction) of the animal
local minSize = 5
local maxSize = 25

--- ---------------------- Start Set Multiplier -------------------

--- **Set Bait Multiplier**
---@param multiplier int
local function setBaitMultiplier(multiplier)
    bait.setCheese(cheese * multiplier)
    bait.setChocolate(chocolate * multiplier)
    bait.setPeanutButter(peanutButter * multiplier)
    bait.setProcessedCheese(processedCheese * multiplier)
    bait.setTomato(tomato * multiplier)
end

--- **Set Trap Multiplier**
---@param multiplier int
local function setTrapMultiplier(multiplier)
    trap.setTrapMouse(trapMouse * multiplier)
end

--- **Set Zone Multiplier**
---@param multiplier int
local function setZoneMultiplier(multiplier)
    zone.setDeepForest(deepForest * multiplier)
    zone.setFarmLand(farmLand * multiplier)
    zone.setForest(forest * multiplier)
    zone.setTownZone(townZone * multiplier)
    zone.setTrailerPark(trailerPark * multiplier)
    zone.setVegetation(vegetation * multiplier)
end

--- **Set "size" Multiplier**
--- Min and max "size" (understand hunger reduction) of the animal
---@param multiplier int
local function setSizeAnimalMultiplier(multiplier)
    rat.minSize = minSize * multiplier;
    rat.maxSize = maxSize * multiplier;
end

--- ---------------------- Start init Bait -------------------

--- **Init Bait**
local function initBait()
    creatureFactory.createBait(rat, bait.bait.CHEESE, bait.getCheese())
    creatureFactory.createBait(rat, bait.bait.CHOCOLATE, bait.getChocolate())
    creatureFactory.createBait(rat, bait.bait.PEANUT_BUTTER, bait.getPeanutButter())
    creatureFactory.createBait(rat, bait.bait.PROCESSED_CHEESE, bait.getProcessedCheese())
    creatureFactory.createBait(rat, bait.bait.TOMATO, bait.getTomato())
end

--- **Init Trap**
local function initTrap()
    creatureFactory.createTrap(rat, trap.trap.MOUSE, trap.getTrapMouse())
end

--- **Init Zone**
local function initZone()
    creatureFactory.createZone(rat, zone.zone.DEEP_FOREST, zone.getDeepForest())
    creatureFactory.createZone(rat, zone.zone.FOREST, zone.getForest())
    creatureFactory.createZone(rat, zone.zone.TOWN_ZONE, zone.getTownZone())
    creatureFactory.createZone(rat, zone.zone.TRAILER_PARK, zone.getTrailerPark())
    creatureFactory.createZone(rat, zone.zone.VEGETATION, zone.getVegetation())
end

--- **Init**
local function init()
    local multiplier = SandboxVars.TrappingSurvival.Rat
    rat = creatureFactory.creature(type, strength, item, maxSize, minSize, minHour, maxHour)

    --                  ** MULTIPLIER **
    setBaitMultiplier(multiplier)
    setTrapMultiplier(multiplier)
    setZoneMultiplier(multiplier)
    -- setSizeAnimalMultiplier(multiplier)

    --                  ** Init bait/trap/zone **
    initBait()
    initTrap()
    initZone()

end

---**Get Rat**
---@return table Rat
function getRat()
    init()
    return rat
end
